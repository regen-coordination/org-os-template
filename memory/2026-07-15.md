# 2026-07-15 — v0.5 Cross-Instance Consolidation

**Operator:** Luiz Fernando
**Session type:** framework consolidation (thorough pass pulling latest instance developments upstream into v0.5)

## Focus

Consolidate all latest developments from the instances (skills, integrations, modules) into the org-os framework, and re-establish the framework as the most up-to-date node. Method: 4 parallel audit agents (refi-bcn-os, refi-dao-os, regen-coordination-os, cross-cutting) over the `npm run analyze:instances` baseline (27 drift items), then verified promotion.

## Promoted into the framework

**Skills (skills/):**
- `research` v1.1.0 — Feynman research harness. All 3 instance copies were byte-identical (refi-bcn origin); took refi-bcn's (newest, +Browser Capability section), genericized ReFi sections, fixed invalid YAML frontmatter (`*.provenance.md` unquoted). Matrix: candidate → canonical.
- `working-with-obsidian-canvas` (from refi-dao-os) — JSONCanvas read/generate/validate/merge, ships validate-canvas.mjs + diff-canvas.mjs + 3 reference docs. Genericized instance references.
- `web-browsing` (from refi-bcn-os) — already written portable; + `scripts/browse-check.mjs` + `docs/integrations/BROWSE-INTEGRATION.md`.
- `notion-cli` (vendored MIT, makenotion/skills @423af2b) — verbatim with LICENSE.

**Commands (.claude/commands/):** `/commit`, `/sync`, `/handoff` (refi-bcn, committed there same day) — genericized operator names → roster/slug resolution.

**Cross-editor command mechanism:** `scripts/sync-commands.mjs` (+ `npm run sync:commands`, `.prettierignore`) — mirrors `.claude/commands/` → `.opencode/commands/`, `.cursor/commands/`, `skills/commands/<name>/SKILL.md` (Hermes/Zed). Generated 17 files from 6 canonical commands. Solves the Zed/claude-acp project-commands gap (see user-memory note).

**Multi-operator trunk model:** `scripts/operator-setup.sh` (parameterized to any slug) + `scripts/hooks/pre-commit` (blocks commits to main via core.hooksPath).

**Script backports (verified by diff, not by date):**
- `generate-all-schemas.mjs` — hand-MERGED (framework was richer in 5 generators; instances had 4 real improvements): YAML identity parse of federation.yaml (was regex), `generateDao()` from dao.json.template (description genericized — instance had hardcoded "ReFi DAO" string), meetings.yaml+dir merge, robust @handle/#tag regexes, sources[] added to knowledge.json.
- `clone-linked-repos.mjs` — copied from refi-bcn (submodule-aware, optional-repo handling, error collection).
- `setup-org-os.mjs` — NOT backported: framework is ahead (v3.1 egregore path is framework-only). Date-based "instance newer" signal was wrong; content diff said otherwise.

**Docs:** `docs/DATA-MODEL.md` +"Recognized Extension Registries" section — hermes-cron.yaml declarative cron convention (bcn+dao), Hub-type federation registries (nodes/funds/initiatives/programs from regen-coordination-os, as templated shapes), orgs.yaml + figma-assets.yaml noted.

## Framework self-fixes

- `federation.yaml`: version/spec labels `3.5` → `0.5` (re-baseline had missed them); `agent.skills` reconciled 21 → 32 (7 missing existing + 4 promotions; generated skills/commands/ intentionally excluded); bread-coop-os added to `downstream:` (was missing); metadata.last_updated → 2026-07-15.
- `validate-structure.mjs`: skills/commands/ recognized as generated command-skill container (checks children instead of failing).
- Matrices: skills-matrix +7 entries (research→canonical; working-with-obsidian-canvas, web-browsing, notion-cli, commands canonical; notion-sync, symbient instance-specific) → 40 entries. packages-matrix +4 (maps, hub, coop, regen-toolkit — all instance-specific) → 22 entries; paperclip-agents-app annotated (regen fork AHEAD, review pending).
- `data/instances.yaml`: declared all instance extras (skills_extra, packages, data_registries_extra) per audit.

## Result

- Drift: **27 → 0** across all 7 instances (verified re-run).
- Validation: schemas 14/0, structure 52/0 (2 benign warnings), tests 23/0.
- All new/modified scripts syntax-checked.

## Deliberately NOT backported

- refi-bcn `AGENTS.md` (2mo newer) — turned out to be a full instance rewrite (ReFi BCN persona, Telegram roles, Celo identity), not a newer framework doc. The "chat-gateway persona runs /initialize on session activation" pattern is worth a future AGENTIC-ARCHITECTURE note.
- refi-bcn `initialize.mjs` (+800 lines vault-safe hardening) — massively instance-diverged; feature-level backport queued in HEARTBEAT.
- `packages/coop`, `packages/regen-toolkit`, `packages/maps`, symbient, notion-sync — instance products/experiments; registered in matrices only.

## Follow-ups (queued in HEARTBEAT → "Consolidation follow-ups")

initialize.mjs reconciliation · paperclip-agents-app backport review · Figma/OKLCH token scripts evaluation · close/initialize command-body backport · hub data-bridge pattern doc · coop-os nested-workspace cleanup (517 dirty files — vault-safe methods only) · propagate v0.5 downstream on each instance's next sync.

## Key decisions

- Date-newer ≠ content-newer: always diff before backporting (setup-org-os.mjs would have been a regression).
- Generated `skills/commands/` is excluded from `federation.yaml agent.skills` and special-cased in validate-structure — generated artifacts are registered via their mechanism, not enumerated.
- Hub-type registries promoted as documented shapes (DATA-MODEL.md), not as populated data files.
