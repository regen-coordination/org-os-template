# 2026-05-02

## Session — 19:58

**Focus:** Diagnose why Zed's agent threads tab labels org-os work as "03 Libraries" when the org-os repo is opened directly.

### Key Findings
- `03 Libraries/org-os` is a **git submodule** of the parent vault, not a standalone repo.
- `org-os/.git` is a pointer file: `gitdir: ../../.git/modules/03 Libraries/org-os`.
- Real gitdir: `Zettelkasten/.git/modules/03 Libraries/org-os/` with `worktree = ../../../../03 Libraries/org-os`.
- `git rev-parse --show-superproject-working-tree` correctly reports the parent vault.
- Zed's agent-threads scope keys off either the resolved gitdir path or the detected superproject — both contain "03 Libraries", which is why threads get that label. Not a misconfiguration; standard submodule behavior + a Zed UX choice.
- Same root cause applies to all `03 Libraries/*-os/` submodules (refi-bcn-os, refi-dao-os, regen-coordination-os, refi-med-os).

### Actions Taken
- [x] Inspected `.git` pointer file and `.git/modules/03 Libraries/org-os/config`.
- [x] Confirmed via `git rev-parse --show-toplevel`, `--git-dir`, `--show-superproject-working-tree`.
- [x] Explained submodule layout vs standalone-repo layout.

### Next
- [ ] If the cosmetic issue becomes annoying: investigate Zed settings for an "ignore superproject" / "treat as standalone" toggle.
- [ ] De-submoduling org instances would fix it but is gated by VAULT-SAFETY (requires snapshot + explicit user approval). Not pursuing now.
