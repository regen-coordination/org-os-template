# 2026-08-29 — v0.5.0 ships

**Focus:** Resume after the WS-H acceptance failure, execute the claim-narrowing
decision, re-run the narrowed acceptance, and cut the `v0.5.0` tag.

## The decision that unblocked the release

WS-H stopped the tag on 2026-08-28: a full `doctor sync` cannot work against
any real instance, because all six are scaffolds with their own root commit and
`sync-upstream.mjs`'s rebase assumes fork lineage
(`memory/reports/ws-h-acceptance-2026-08-28.md`). Operator decision today:
**option 2** — narrow the v0.5 claim to `assess` + `sync --dry-run` (both
proven), ship the full sync documented as unproven, redesign it as a
**file-level overlay** in v0.5.1, then re-run WS-H H1–H3 in full. Recorded as
its own DECISIONS.md entry; every claim surface amended (CHANGELOG Added bullet
+ third Known issue, SKILL.md status callout, package README, masterplan second
amendment) in `bb60ee6`.

## Narrowed acceptance — PASSED

Full record: `memory/reports/ws-h-acceptance-narrowed-2026-08-29.md`.

- Framework self-assess: **0 blockers** (was 3 the previous morning; fixed by
  taking its own report seriously).
- `doctor assess` across all six instances: honest scorecards, blocker exit
  codes as designed (refi-med 1/7 — down from 2/12 after its recovery repairs;
  bread-coop 8/9 incl. the missing-remote BLOCKER with hint; regen-coordination
  9/11 incl. template-leakage + dup-key; refi-bcn 4/9; refi-dao 4/13; dao-os
  3/11).
- `doctor sync --dry-run` against the two dirty production trees (refi-bcn,
  refi-dao): exit 0, full 9-stage plans, zero mutation.

## Shipped

- Tag **`v0.5.0`** (annotated) on `main`, pushed with `--follow-tags` after a
  final fetch + fast-forward check. The drift and generate-schemas crons (both
  commit straight to main; the drift bot raced this release once already —
  merge `6930b0b`) were **paused via `gh workflow disable` for the ship window
  and re-enabled after** — check they are `active` if this note is old.
- Historical tags stand as `archive/v3.0.0` / `archive/v3.5.0` only; the bare
  `v3.x` tags were already removed from origin and local. **Never re-push bare
  v3 tags** — they outrank `v0.5.0` in semver-sorted lists.
- DECISIONS.md: the ship entry (five convergence decisions as executed) + the
  claim-narrowing decision.
- Gate suite green on `main` at tag time; validate.yml CI green on the release
  candidate; `vault:snapshot` at the release point; `vault:audit` after.

## G4 — drafted and presented, NOT applied (operator approval pending)

1. `AGENTS.md` §11 upstream link fix (`org-os-framework` → `org-os-template`)
   — protected core file, presented as a draft.
2. Proposal: archive the live, divergent `organizational-os-framework` GitHub
   repo with a pointer README to `org-os-template`. External repo action,
   operator-only.

## Carry-forward

- **v0.5.1:** file-level overlay sync + WS-H H1–H3 full re-run · kms data-loss
  fixes (F4) · admin M1 defect bundle from the 2026-08-28 review pass (projects
  enum 422s on real data, funding topKey dual-list data-loss path, YAML reflow,
  stale form draft, git index.lock race — findings recorded in the session's
  review, decision pending on fix-before-demo vs v0.5.1).
- **v0.6 Active-1:** fleet propagation, gated on F4.
- **refi-med-os** carries 5 local unpushed commits from the recovery (3
  redundant machinery installs left un-rewritten by policy).
- **WS-I / H7** next: adoption kit + session artifacts for the ~2026-09-10
  Regen Knowledge Commons session; I6 clean-room re-run is now unblocked by the
  tag.
