# 2026-08-20 — Session Log

## Session: Berd agent integration (Upstream, via Berd)

**Focus:** Integrate Berd agents into org-os — the two org-os personas (Operator,
Upstream) existed only as untracked files in `~/.agents/agents/`.

**Done (branch `feat/berd-agents`, from `autopoiesis-phase2-pilot` HEAD):**

- `.agents/agents/{operator,upstream}.md` — canonical personas in-repo, seeded
  verbatim from the hand-authored globals plus a `managed_by: org-os` marker.
  Berd auto-discovers this dir when the project is open.
- `scripts/sync-agents.mjs` + `npm run sync:agents` — one-way mirror repo →
  `~/.agents/agents/`, marker-guarded (only overwrites `managed_by: org-os`
  files; `--adopt` for hand-authored takeover; Berd-bundled agents never
  touched; `--dry-run`; `--source/--target` for tests). Pattern mirrors
  `sync-commands.mjs`.
- `tests/scripts/sync-agents.test.mjs` — 10 cases, TDD (red first). Full suite
  122/122; selftest 6/6; prettier clean.
- Spec moved with implementation: `docs/AGENTIC-ARCHITECTURE.md` (Berd in Agent
  Runtimes + new "Berd Personas" subsection), `docs/FILE-STRUCTURE.md`
  (`.agents/` entry).
- Ran `sync:agents --adopt` for real: both globals adopted, now byte-identical
  to canon. Decision recorded in `DECISIONS.md` (2026-08-20).

**Open:**

- Merge path for `feat/berd-agents` (branched off the pilot branch, which
  carries interleaved commits — same caveat as the autopoiesis merge decision).
- Follow-up candidate: Operator's persona hard-codes this deployment's vault
  paths; a template-ized persona for fresh instances is deliberately not built
  yet (YAGNI until a second deployment wants it).
- Berd remains a single-deployment surface — one machine, one operator. Per
  pattern rules this is framework _infrastructure_ (optional dir + script), not
  a promoted pattern; revisit if a second operator adopts Berd.
