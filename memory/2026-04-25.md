# 2026-04-25 — v3.5 Release: Ready for Real Orgs

**Operator:** Luiz Fernando
**Session type:** release (v3.5.0 ship)
**Mode:** auto (subagent-driven)

---

## What happened

Shipped org-os v3.5.0. The release that takes org-os from "self-hosting framework"
to "real orgs can clone and operate." Five sub-plans landed:

1. **Cloning engine** — `scripts/clone-framework.mjs` is the keystone. Single
   command: identity capture → strip framework-only artifacts → render templates
   → materialize packages → npm install + validators → git init. Producing a
   passing instance in under a minute. GitHub Template wrapping in place.

2. **Package consumption** — `scripts/sync-packages.mjs` materializes vendored
   packages per `federation.yaml` toggles, pinned to `framework_version`.
   `dashboard` package promoted to framework as the first real consumer.

3. **Reliability layer** — pre-commit hook + CI workflow (`.github/workflows/
   validate.yml`) + scheduled drift workflow + `npm run selftest` aggregator.
   Federation SLA: 30 days drifted, 90 days dormant.

4. **One-pager templates** — `templates/README.framework.md`,
   `templates/README.instance.md`, `templates/GETTING-STARTED.md`, all rendered
   via a minimal Mustache-style engine. Framework README replaced;
   `GETTING-STARTED.md` is the new conversational onboarding artifact.

5. **bread-coop-os live** — bootstrapped via the engine, validators pass,
   dashboard rendering, on its own main branch with a clean bootstrap commit.
   The acceptance test passed.

---

## Decisions logged

(See `DECISIONS.md` for the full list — 8 decisions captured during the
2026-04-25 brainstorming session.)

---

## Files of note

- New scripts: `clone-framework.mjs`, `sync-packages.mjs`, `selftest.mjs`,
  `install-hooks.mjs`, `bootstrap-collect.mjs`, `render-self.mjs`
- New docs: `PACKAGE-LIFECYCLE.md`, `RELIABILITY.md`
- New templates: `README.framework.md`, `README.instance.md`,
  `GETTING-STARTED.md`, `templates/render.mjs`
- New workflows: `validate.yml`, `drift.yml`, `template-cleanup.yml`
- New instance: bread-coop-os (separate repo, sibling of org-os)
- Updated registries: `instances.yaml` (+ bread-coop-os), `packages-matrix.yaml`
  (+ lifecycle_status field, dashboard promoted to framework)

---

## Acceptance test results

- Cloning engine: end-to-end successful on bread-coop-os
- All 11 stages completed: copy → strip → reset → bootstrap answers → render →
  materialize skills → write federation.yaml → sync-packages → npm install →
  generate:schemas → validate:schemas + validate:structure (PASS) →
  git init + initial commit
- bread-coop-os/main has bootstrap commit `533fc13`
- Validators on bread-coop-os: 48 passed / 0 failed / 2 warnings (clean)

---

## Next session

- Begin v3.6: `obsidian-interface` plan moves to active
- Continue `obsidian-canvas-interface` scoping in parallel
- After v3.6: `non-tech-onboarding` web wrapper over the v3.5 engine
- v3.7: TUI dashboard, web dashboard as thin renderer
- Selective npm publishing of stable framework packages

---

## Session — 2026-04-25 (release)

**Focus:** Ship v3.5.0 via subagent-driven development.

### Actions taken

37 tasks across 4 phases executed via subagent dispatch with two-stage review
(spec compliance + code quality). Two parallel agents: main session for Phase
1a + 1b + Phase 2 + Phase 3 critical path; Hermes for Phase 1c templates +
Phase 3/4 docs prep.

Phase 1a (Reliability, 6 tasks): audit, selftest, version:check, pre-commit,
CI, drift workflows.

Phase 1b (Package Integration, 6 tasks): audit, lifecycle_status field,
PACKAGE-LIFECYCLE.md, sync-packages.mjs, sync-upstream wiring, dashboard ready.

Phase 1c (Templates, 6 tasks): renderer, partials, READMEs,
GETTING-STARTED, render-self. Hermes also fixed the framework's pre-existing
structural drift (.well-known/dao.json + federation wrapper) as bonus.

Phase 2 (Cloning Engine, 10 tasks): scaffold, copy/strip, reset markdown,
bootstrap-collect, render templates, materialize, federation+sync, install+
validate, git init, GitHub Template wrapping. Plus a critical fix for nested
federation.yaml schema + generate:schemas in pipeline.

Phase 3 (bread-coop-os, 5 tasks): fixture, bootstrap, register in instances,
re-validate (deferred), dashboard package physical promotion.

Phase 4 (Release, 4 tasks): CHANGELOG (Hermes), version bump 3.0.0 → 3.5.0,
plans/HEARTBEAT/memory closeout (this entry), verification gate review.

### Next

- [ ] Push v3.5.0 tag publicly
- [ ] Communicate release to instances (their next /initialize will surface
      migration if needed)
- [ ] Open v3.6 brainstorming session for obsidian-interface
- [ ] Push bread-coop-os to a real GitHub repo when ready
