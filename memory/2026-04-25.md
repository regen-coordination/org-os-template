# 2026-04-25 — Three new workstreams scoped and queued

**Operator:** Luiz Fernando
**Session type:** scoping (brainstorming → plan-writing)
**Mode:** interactive

---

## Session — 16:14

**Focus:** Scope and queue three new framework workstreams: package integration, system reliability, and instance bootstrap pipeline.

### Key Decisions

- **Three new workstreams chosen over folding into existing ones.** Each is substantial enough (multi-phase, multi-session) to warrant first-class status. Mirrors the `skill-promotion` precedent.
- **`instance-bootstrap` is the engine; `non-tech-onboarding` becomes the UI wrapper on top.** Two boundary alternatives rejected: (a) absorbing non-tech-onboarding (would over-couple CLI and web concerns), (b) parallel independent plans (would risk divergent implementations). The engine-and-wrapper model creates a natural dependency and lets non-tech-onboarding shrink to a tighter, more shippable scope.
- **Plan shape standardized** — all three plans follow the same template: Goal → Open Questions → Phased Tasks (3 phases) → Side Effects → Verification → Splitting Criteria. Splitting Criteria is the explicit escape hatch for plans that grow during execution.
- **Promoted scoping → queued despite open questions.** Phase 1 of each plan (audit) is concrete and doesn't need open-question resolution. Phase 2/3 questions get answered as those phases activate.

### Plans created (all status: queued)

| Plan | Workstream | Priority | Sessions |
|---|---|---|---|
| `package-integration` | package-integration (new) | 4 | 3 |
| `system-reliability` | reliability (new) | 5 | 3 |
| `instance-bootstrap` | instance-bootstrap (new) | 6 | 4 |

### Actions Taken

- [x] Brainstormed package integration plan (3 questions: scope = D-all-of-above, mechanism = E-defer, workstream = A-new)
- [x] Brainstormed reliability plan (1 question: trigger model = recommended layered approach)
- [x] Brainstormed instance bootstrap plan (2 questions: boundary with non-tech-onboarding = A-engine, knowledge scope = B-proof-of-pipeline)
- [x] Wrote `docs/agent-plans/package-integration.md`
- [x] Wrote `docs/agent-plans/system-reliability.md`
- [x] Wrote `docs/agent-plans/instance-bootstrap.md`
- [x] Added 3 workstream rows to `data/projects.yaml`
- [x] Promoted all 3 plans scoping → queued (priorities 4, 5, 6; estimated_sessions set)
- [x] Updated `docs/agent-plans/QUEUE.md` queued + scoping sections
- [x] Patched `non-tech-onboarding.md` frontmatter: `depends_on: [future-instance-specs, instance-bootstrap]`

### Verified during /initialize

- [x] `/initialize` renders real content with no stubs (verified — header, projects, tasks, instances, candidates all populated from registries)
- [x] `scripts/initialize.mjs` emits valid JSON with populated registries (verified)

### Next

- [ ] Activate `package-integration` plan phase 1 — inventory audit of `packages/` against `packages-matrix.yaml`
- [ ] Activate `system-reliability` plan phase 1 — inventory audit of existing reliability infra
- [ ] Activate `instance-bootstrap` plan phase 1 — resolve cloning mechanism (open question 1)
- [ ] Resolve `regen-coordination-os` not-cloned state (still pending from 2026-04-24)
- [ ] Reconcile `federation.yaml` `agent.skills` with actual `skills/` directory (still pending)
