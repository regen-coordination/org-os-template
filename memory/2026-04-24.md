# 2026-04-24 — Self-Hosting Inauguration

**Operator:** Luiz Fernando
**Session type:** major transition (not routine `/initialize`)
**Mode:** auto (continuous execution)

---

## What happened

The org-os repo converted from a template-with-stubs into a **live self-hosting instance** that also operates as the **multi-instance orchestration hub** for all downstream instances.

Before today: 11 canonical protocol files existed at root (SOUL, IDENTITY, USER, HEARTBEAT, MEMORY, TOOLS, AGENTS, MASTERPLAN, BOOTSTRAP, CLAUDE, federation.yaml) but most were template placeholders. `data/*.yaml` were empty arrays. Only `MASTERPLAN.md` carried real content.

Today: stubs filled with real org-os-development content; all 13 canonical data registries populated (where applicable to the solo-maintainer phase); three new framework-only registries introduced; federation wired to map every downstream instance; dashboard tuned for framework/orchestration view.

---

## Decisions (logged to MEMORY.md)

- **Self-hosting inauguration** — org-os runs org-os on itself; also orchestrates downstream instances.
- **Projects-vs-plans separation** — `data/projects.yaml` holds long-lived workstreams; `docs/agent-plans/` holds specific plans. Plans gain `workstream:` frontmatter field.
- **Identity trajectory** — solo-maintainer → OSS → DAO. Governance/treasury fields kept present but marked `N/A (solo phase)`. Triggers documented in `IDENTITY.md` → Evolution Triggers.
- **Framework-only registries** — `instances.yaml`, `skills-matrix.yaml`, `packages-matrix.yaml` added as framework-only. Documented in `docs/DATA-MODEL.md`. Individual instances do NOT need them.
- **Skill promotion policy** — `docs/SKILL-PROMOTION.md` defines criteria (≥2 instances validating the pattern), workflow, and non-criteria.

---

## Ecosystem mapped (5 known instances)

| Instance | Type | Maturity | Cloned | Drift |
|---|---|---|---|---|
| `refi-bcn-os` | LocalNode | production | yes | none on inauguration |
| `refi-dao-os` | DAO | production | yes | none on inauguration |
| `dao-os` | Project (dev platform) | beta | yes | no_masterplan (expected for dev platform) |
| `openclaw` | AgentRuntime | alpha | yes | no_federation_yaml, no_masterplan, stub_identity (expected — not a data instance) |
| `regen-coordination-os` | Hub | unknown | no | not_cloned_locally (pending resolution) |

Supporting repos (not org-os instances): `ReFi-Barcelona` (knowledge base), `ReFi-BCN-Website`, `Regenerant-Catalunya`, `Local-ReFi-Toolkit`, `organizational-os-framework`, `organizational-os`.

---

## Promotion candidates surfaced

See `data/skills-matrix.yaml` for authoritative list.

- `research` — in refi-bcn-os + refi-dao-os (≥ 2 instances, criterion met) → **promote next**
- `safe-treasury`, `hats-governance`, `gardens-governance`, `karma-reputation`, `eip4824-identity` — all in dao-os → candidates; framework treasury/governance-protocol layer currently missing
- `karpathy-guidelines` — only refi-dao-os → evaluating
- `dashboard` (package) — in refi-bcn-os + refi-dao-os → candidate; scoping plan `framework-dashboard-template` exists

---

## Files changed

Filled / rewritten: `SOUL.md`, `IDENTITY.md`, `USER.md`, `HEARTBEAT.md`, `MEMORY.md`, `BOOTSTRAP.md`, `federation.yaml`, `dashboard.yaml`.

Data populated: `data/members.yaml`, `data/projects.yaml`, `data/ideas.yaml`, `data/governance.yaml`, `data/relationships.yaml`.

New framework-only registries: `data/instances.yaml`, `data/skills-matrix.yaml`, `data/packages-matrix.yaml`.

New scripts / docs: `scripts/analyze-instances.mjs`, `docs/SKILL-PROMOTION.md`, `docs/agent-plans/README.md`. `docs/DATA-MODEL.md` gained "Framework-only registries" section. `package.json` gained `analyze:instances` script.

Plan frontmatter: all 5 plan files in `docs/agent-plans/` gained `workstream:` field.

Memory: this file.

---

## Next session

- Verify `/initialize` renders real content in a fresh session.
- Begin executing `skill-promotion` workstream — start with `research` skill (highest-confidence promotion candidate).
- Resolve `regen-coordination-os` local absence (clone or deprecate).
- Review `federation-protocol` queued plan — ready to move to active.

---

## Session — 23:57

**Focus:** Multi-instance orchestration hub (full execution of the /initialize–approved plan) + three new scoping plans (Obsidian interface, Obsidian Canvas, versioning) + full execution of the versioning-system plan including v3.0.0 bump.

### Key Decisions

All logged to `DECISIONS.md` (authoritative). New today:
- Versioning system (single source of truth, decoupled schema/framework/skill versions, pull-based migrations, strict semver).
- Self-hosting inauguration.
- Projects-vs-plans separation (plans carry `workstream:` frontmatter).
- Identity trajectory solo → OSS → DAO.
- Framework-only registries (`instances.yaml`, `skills-matrix.yaml`, `packages-matrix.yaml`).
- Skill promotion policy (≥2-instance criterion).

### Actions Taken

**Self-hosting & orchestration hub:**
- [x] Filled SOUL.md, IDENTITY.md, USER.md, HEARTBEAT.md with real content.
- [x] Populated 5 canonical data registries (members, projects, ideas, governance, relationships).
- [x] Introduced 3 framework-only registries (instances, skills-matrix, packages-matrix) covering all 5 known instances.
- [x] Wrote `scripts/analyze-instances.mjs` (`npm run analyze:instances`) — produces drift report.
- [x] Wrote `docs/SKILL-PROMOTION.md` + extended `docs/DATA-MODEL.md` with framework-only section.
- [x] Rewired `federation.yaml` to framework-upstream + orchestration-hub role.
- [x] Tuned `dashboard.yaml` for framework view with `instances` and `skill-promotion-candidates` custom sections.
- [x] Extended `scripts/initialize.mjs` to load ideas, instances, skill candidates.
- [x] Added `workstream:` frontmatter to all 5 existing plan files; created `docs/agent-plans/README.md`.

**New scoping plans (3):**
- [x] `obsidian-interface` — Obsidian as primary operator interface (candidate package).
- [x] `obsidian-canvas-interface` — Canvas as system overview + interface (depends on obsidian-interface).
- [x] `versioning-system` — promoted from scoping → completed same session.

**Versioning system (full execution):**
- [x] `docs/VERSIONING.md` policy codified (7 scoping Qs answered).
- [x] `CHANGELOG.md` created, v2.0.0 + v3.0.0 entries written.
- [x] `package.json.version`: 2.0.0 → 3.0.0.
- [x] `scripts/update-version.mjs` (fixes broken reference).
- [x] `scripts/migrate.mjs` + `scripts/migrations/v2-to-v3-workstream-frontmatter.mjs` (idempotent, tested).
- [x] `docs/migrations/v2-to-v3.md` operator guide.
- [x] `scripts/validate-structure.mjs` new section 8: Version Consistency (3/3 pass).

### Next

- [ ] Push commit (tag kept local per instruction).
- [ ] Begin `skill-promotion` workstream with `research` skill.
- [ ] Resolve detached HEAD situation (branch is currently HEAD).
- [ ] Move `federation-protocol` queued plan → active next session.
- [ ] Next release: edit `CHANGELOG.md [Unreleased]` before running `npm run version:update`.
