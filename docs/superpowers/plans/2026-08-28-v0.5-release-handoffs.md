# v0.5 Release — Execution Handoffs

> Six copy-paste session briefs executing the [v0.5 release masterplan](2026-08-28-v0.5-release-masterplan.md). Paste one per fresh agent session (Claude Code / OpenCode / Cursor / Zed) in a clone of `regen-coordination/org-os-template`.
>
> **Sequencing:** H1 ∥ H2 ∥ H3 may run in parallel (H2 works on its own branch). H4 requires H1 done. H5 requires H1 + H2 done. H6 runs last.
>
> Every brief embeds the three safety rules a fresh session must know: (1) never `git stash` / `git reset --hard` / the untracked-file-deleting git verb; (2) the pre-commit guard blocks any *command line* containing both "git" and the bare word "clean" — even in messages or filenames — use `git commit -F <file>` when needed; (3) stage explicit paths only, never `-A`. Concurrent sessions exist: **re-verify HEAD, branch list, and PR state before acting; if reality diverges from the masterplan's §0, stop and reconcile the masterplan first.**

## H1 — WS-A: Land admin M1 (PR #1)

```
In org-os (03 Libraries/org-os): fetch, switch to main, fast-forward from origin/main. You are executing workstream WS-A of docs/superpowers/plans/2026-08-28-v0.5-release-masterplan.md — read its header, §0, §2 preamble, and §WS-A now, and follow WS-A task-by-task. Safety: never stash or hard-reset; the pre-commit guard blocks any command line containing both "git" and the bare word "clean" (use git commit -F <file> to work around); stage explicit paths only; snapshot with npm run vault:snapshot before each merge. Re-verify `gh pr view 1` is still OPEN and .worktrees/admin-app still exists before starting. Deliverable: feat/admin-app refreshed from main (union rule on conflicts), admin tests + full gate suite green, the admin-app design spec promoted out of .worktrees/admin-app into docs/superpowers/specs/, PR #1 merged into main as a merge commit (no squash), gates green on main, worktree removed, branch archived as archive/feat-admin-app and deleted local+origin. Tick the WS-A checkboxes in the masterplan, commit with explicit paths, push main and fast-forward feat/berd-agents to main and push it too.
```

## H2 — WS-B: `packages/instance-doctor/`

```
In org-os (03 Libraries/org-os): fetch, switch to main, fast-forward from origin/main, then create branch feat/instance-doctor from main. You are executing workstream WS-B of docs/superpowers/plans/2026-08-28-v0.5-release-masterplan.md — read its header, §0 (especially the instance field state and gap #2), and §WS-B, then build task-by-task with strict TDD (test first, node:test, fixtures under the package). Safety: never stash or hard-reset; the pre-commit guard blocks any command line containing both "git" and the bare word "clean" (use git commit -F <file>); stage explicit paths only. Deliverable: packages/instance-doctor/ with the 12-check assessment (B1–B7: identity coherence + template-leakage, lineage, cross-scheme version surfaces, machinery integrity incl. missing/stub scripts and absent remotes, structure+schemas reuse, freshness, scorecard with --json and blocker exit codes) and the sync verbs (B8–B9: snapshot → ensure upstream remote → inject sync machinery → run the existing scripts/sync-upstream.mjs → migrate → regenerate → re-assess → receipt + lineage stamp; --dry-run; abort-safe staging), plus skills/instance-doctor/SKILL.md, npm run doctor, and registration in packages-matrix, skills-matrix, federation.yaml agent.skills (B10–B11). Fixtures must model the six real instances' failure signatures listed in the masterplan §0. Full suite green via npm test. Commit incrementally with explicit paths, push the branch, open a PR to main titled "feat(instance-doctor): assessment + reliable sync (v0.5 WS-B)", and tick the WS-B checkboxes in the masterplan in the PR.
```

## H3 — WS-D: Site live on GitHub Pages

```
In org-os (03 Libraries/org-os): fetch, switch to main, fast-forward from origin/main. You are executing workstream WS-D of docs/superpowers/plans/2026-08-28-v0.5-release-masterplan.md — read its header and §WS-D, then execute D1–D4. Safety: never stash or hard-reset; the pre-commit guard blocks any command line containing both "git" and the bare word "clean"; stage explicit paths only. Decisions are locked: deploy repo regen-coordination/org-os-template, URL https://regen-coordination.github.io/org-os-template/ (base /org-os-template), current theme. Deliverable: astro.config.mjs site+base set; site/src/lib/base.ts withBase() helper; every root-absolute href/src/fetch in site/src routed through it (known: landing CTA renderer, the federation-map src="/map.json" runtime fetch, nav/footer, verify-build.mjs expectations) with the gate that built dist/index.html contains zero unprefixed internal links; .github/workflows/deploy-pages.yml (root npm ci BEFORE site npm ci — site imports ../../packages/org-os-kms; verify the build degrades gracefully with no sibling instance clones); Pages enabled via gh api (build_type=actions; 409 means already enabled, use PUT); first run watched, live URL verified (all top routes 200, hero copy present, /org-os-template/map.json 200); repo homepage set via gh repo edit and the live URL linked from README (edit templates/README.framework.md + npm run render:templates if that is the source). Site build + tests green locally before pushing. Tick the WS-D checkboxes in the masterplan, commit with explicit paths, push main and fast-forward feat/berd-agents to it, push both.
```

## H4 — WS-E: Branch/worktree topology (run AFTER H1)

```
In org-os (03 Libraries/org-os): fetch, switch to main, fast-forward from origin/main. You are executing workstream WS-E of docs/superpowers/plans/2026-08-28-v0.5-release-masterplan.md — read its header, §WS-E's table, and memory/reports/branch-triage-2026-08-21.md for the precedent, then execute E1–E3. Preconditions: verify WS-A completed (PR #1 merged, archive/feat-admin-app exists) — if not, stop. Safety: this session is pure git topology — snapshot first (npm run vault:snapshot -- "before WS-E"); never stash or hard-reset; the pre-commit guard blocks any command line containing both "git" and the bare word "clean"; before any worktree removal copy out non-regenerable untracked files (08-21 precedent); every deletion must be preceded by a pushed archive/* tag and a recorded rev-list containment count. Deliverable: execute the WS-E table exactly (docs-prep/templates/consolidation-backup tag+delete; ORG-4's two doc commits cherry-picked then tag+delete; knowledge-commons, rad-org-os, kms-connector-layer, tech-tree pushed then tag+delete with worktrees removed; .claude/worktrees/v05-main removed; stale origin-only refs deleted), the bounded E2 salvage review of archive/v3.5-execution's 983-line clone-framework.mjs + reliability tooling vs main's versions (cherry-pick only clear wins, timebox one session), and E3's end state: git branch shows main (plus feat/berd-agents kept aligned) and zero worktrees, gates green. Write memory/reports/branch-triage-2026-08-28.md with per-branch proof, tick the WS-E checkboxes in the masterplan, commit with explicit paths (note: never put the triage filename in a command line if a path contains the blocked word — none here does), push main + tags.
```

## H5 — WS-C + WS-F remainder: One versioning story + convergence residue (run AFTER H1 and H2)

```
In org-os (03 Libraries/org-os): fetch, switch to main, fast-forward from origin/main. You are executing workstreams WS-C (all) and WS-F tasks F4–F5 of docs/superpowers/plans/2026-08-28-v0.5-release-masterplan.md — read its header, §WS-C, §WS-F, and CHANGELOG.md first. Preconditions: WS-A (admin M1 on main) and WS-B (instance-doctor merged) done — the changelog fold must include both; if either is missing, stop. Safety: never stash or hard-reset; the pre-commit guard blocks any command line containing both "git" and the bare word "clean" (the clean-room report filename triggers it — never put that filename in a command line; use git commit -F <file> for messages); stage explicit paths only. Deliverable: docs/VERSIONING.md gains the 0.x pre-beta policy with 3.x demoted to Historical (C1); CHANGELOG [0.5.0] re-dated to ship day folding everything since 2026-06-17 incl. symbient from [Unreleased] and the new instance-doctor, stub reset (C2); root MASTERPLAN.md version header fixed to 0.5.0 with the stale v2 Activations block replaced by a pointer to the release masterplan (C3, removing the warning banner added 2026-08-28); npm run version:check green; ask the operator before removing MASTERPROMPT.md (C4); the two instance promotion ledgers registered as convergence inputs in docs/SKILL-PROMOTION.md with the two red kms data-loss items already triaged in HEARTBEAT (F4); docs/SETUP-PATHS.md deleted or reduced to an honest stub and README carrying one clear bootstrap recommendation (F5). Tick the WS-C/F boxes in the masterplan, commit with explicit paths, push main and keep feat/berd-agents aligned.
```

## H6 — WS-G + WS-H: Ship + prove it (run LAST)

```
In org-os (03 Libraries/org-os): fetch, switch to main, fast-forward from origin/main. You are executing workstreams WS-G and WS-H of docs/superpowers/plans/2026-08-28-v0.5-release-masterplan.md — read its header, §1 (definition of done), §WS-G, §WS-H. Preconditions: WS-A through WS-F all ticked in the masterplan; if any is open, stop and report. Safety: never stash or hard-reset; the pre-commit guard blocks any command line containing both "git" and the bare word "clean"; stage explicit paths only. Deliverable, in order: full gate suite green on main (npm test, validate:schemas, validate:structure, version:check, selftest, site build+test); npm run vault:snapshot -- "v0.5.0 release point"; DECISIONS.md ship entry confirming the 2026-08-28 convergence decision executed; annotated tag v0.5.0 pushed via git push origin main --follow-tags plus the historical v3.0.0/v3.5.0 tags; memory/2026-08-28.md (or ship-day) release note; npm run vault:audit. Then acceptance: run the instance doctor against ../refi-med-os (expect unstamped/missing-machinery/stale findings, then doctor sync from the framework, re-assess green, receipt + lineage stamped — this is the cross-scheme 3.0→0.5 migration proof) and against ../bread-coop-os (missing git remote must surface as a BLOCKER with a remediation hint, then local sibling sync); assess-only across ../refi-bcn-os, ../refi-dao-os, ../regen-coordination-os, ../dao-os and publish memory/reports/instances-drift-<ship-date>.md from the results. Record acceptance in the release memory note, tick WS-G/WS-H, commit with explicit paths, push. The release claim is only real after H-tasks pass — report honestly if they do not.
```
