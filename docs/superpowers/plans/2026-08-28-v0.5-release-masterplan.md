# org-os v0.5 Beta — Release Masterplan

> **THE convergence point.** Every plan, spec, queue entry, and branch in this repo either ships in v0.5, is explicitly deferred with a trigger, or is superseded — and says so with a pointer to this file. Operator directive 2026-08-28.
>
> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** Ship org-os beta v0.5 as the release that makes the framework **stable and reliable for real adoption and collaborative use**: consolidated trunk, landed admin M1, a new instance-doctor package (full instance assessment + reliable update/sync), live public site, one coherent versioning story, a pruned branch/worktree topology, and every subplan converged on this masterplan.

**Scope of the reliability claim (2026-08-28 amendment):** v0.5 proves assess + sync on three real instances — one pristine, one remote-less, one messy (WS-H) — before the tag is cut. Full-fleet propagation and the newcomer-bootstrap fixes (clean-room B1/B2/M2/M4) are v0.6 Active-1; release copy must not claim them.

**Supersedes / absorbs:**
- Absorbs the open remainder of [`2026-08-21-ship-and-validate.md`](2026-08-21-ship-and-validate.md) (Tasks 4, 10, 11, 12, 15 — its Phases 0–3 + 5 executed 2026-08-21; see `.superpowers/sdd/2026-08-21-ship-and-validate/progress.md`).
- Supersedes [`2026-08-10-v0.5-beta-ship.md`](2026-08-10-v0.5-beta-ship.md) + its spec (written against a pre-08-21 git snapshot; its operator decisions carry forward, its mechanics are folded in here).
- Adopts the [portfolio decision memo](../../agent-plans/2026-08-21-portfolio-decision-memo.md) (Active-3 + freeze table) as release policy, with one amendment: the **instance-doctor package** joins the active set as the vehicle for Active-1 (downstream propagation), per operator directive 2026-08-28.

**Execution:** seven session briefs in [`2026-08-28-v0.5-release-handoffs.md`](2026-08-28-v0.5-release-handoffs.md) — H1(WS-A) ∥ H2(WS-B) ∥ H3(WS-D), then H4(WS-E, after H1+H2 and H3's branch landed — E3 asserts main-only), H5(WS-C+F, after H1+H2), H6(WS-H then WS-G — acceptance before the tag), H7(WS-I adoption kit — content tasks I1/I2/I4 may start anytime; I3 after H3; I6 after H6; session ~2026-09-10).

**Operator decisions locked (2026-08-28):** ship scope = consolidated trunk + public site + instance-doctor · deploy = `regen-coordination/org-os-template` via GitHub Pages project path · release tag = `v0.5.0` · merge strategy = no history rewrite, archive-tags before every deletion · all dev branches/worktrees cleared for the release (content preserved via `archive/*` tags).

**Amended 2026-08-28 late (pre-execution critical review, fixes applied in place):** v05-main checkout deadlock fixed (new A0), `feat/berd-agents` retirement added to WS-E, canonical-upstream URL handling (refi-med's `upstream` is a live *divergent* legacy repo), package-test gate wiring (A5/B11), root `VERSION.md` joins the versioning story (C5), CI soft-fail + site gate (D5), dirty-tree `--dry-run` acceptance (H4), drift-bot race guard (G2), canonical-repo sweep (G4), untracked-report strays (F6). **Second pass (~02:35, two concurrent reviews reconciled):** JSON-conflict rule on the admin refresh (A1) · vault-guard false-positive fix (A6 — retires the "clean" workaround all six briefs teach) · explicit 3.x→0.5 re-stamp inside `doctor sync`'s migrate stage (B8) · gap-9 CI row deduped (D5+A5).

---

## 0. Verified state (2026-08-28 sweep — 3 audit agents + direct git)

**Done (on `main` = `feat/berd-agents` @ `5c7ada3`, all green: 125/125 tests, selftest 6/6, schemas 14/14, structure 53/53, site 17 pages + 15 tests):**
- Trunk landed 2026-08-21: main caught up 292 commits (graphify line + pilot line merged; Critical schema-generator regression found & fixed with test).
- Branch triage round 1: `autopoiesis-phase2-pilot`, `v0.5`, `feat/multica-operator`, `release/v3.5-design`, `release/v3.5-execution` → `archive/*` tags, deleted.
- Positioning truthed-up end-to-end (POSITIONING/README/site hero now honest: single-operator federation disclosed, instance count 5+hub derivable, skill count computed from disk, broken-wizard CTA dropped).
- Clean-room bootstrap experiment run ("Harbor Bakery"): 7 Blockers documented — the newcomer path produces a silently-broken instance that passes all validators. Fix-list in `HEARTBEAT.md`.
- Portfolio memo drafted (Active-3, 16 frozen rows with triggers).
- v0.5 milestone content all on trunk: federation map, kms + quilt, autopoiesis P2 (fixed sync-upstream + validate-identity + lineage stamps), cloudflare-os M0–M2 in-repo, symbient v2, graphify integration, cross-instance consolidation (drift 27→0 on 2026-07-15).

**Open gaps this masterplan closes:**

| # | Gap | Workstream |
|---|-----|------------|
| 1 | `packages/admin/` absent from trunk; PR #1 open since July; admin spec exists only inside `.worktrees/admin-app` | WS-A |
| 2 | **No working sync path into any instance**: `sync-upstream.mjs` missing in refi-bcn/regen/bread (broken `package.json` entries), no-op stub in refi-dao (which also lacks an `upstream` remote); zero sync receipts anywhere; lineage stamps only in bread-coop (+1 prose stamp in refi-bcn) | WS-B |
| 3 | Version chaos: instances say 3.0/3.5 vs framework 0.5.0; `VERSION.md` says 1.0.0 in two instances **and at the framework root** (a *fifth* wrong surface — and `version:check` reads only package.json/federation.yaml/CHANGELOG, so none of the wrong surfaces are machine-caught); root `MASTERPLAN.md` says 2.0.0 (a *fourth* scheme); `docs/VERSIONING.md` still describes 3.x; symbient sits in CHANGELOG `[Unreleased]`; no `v0.5.0` tag | WS-C |
| 4 | Site not deployed: `astro.config.mjs` still `org-os.dev`/no `base`, no Pages workflow, Pages not enabled | WS-D |
| 5 | 9 branches + 7 worktrees remain; frozen work (rad-org-os 55, knowledge-commons 32, tech-tree 21, kms-connector-layer 19, docs-prep/templates contained, ORG-4 2, consolidation-backup 0) untriaged for the release | WS-E |
| 6 | Status debt: QUEUE.md 18 days stale w/ false claims; HEARTBEAT footer stale, ~6 done-but-unchecked items; ~20 spec status lines false; 2 instance promotion ledgers (~24 items, two 🔴 data-loss kms defects) tracked nowhere upstream | WS-F |
| 7 | No release artifacts: tag, folded changelog, decisions entries, release memory note | WS-G |
| 8 | Reliability claim untested: doctor + sync never proven against a real instance | WS-H |
| 9 | CI doesn't enforce the release gate suite: `validate.yml` never runs `npm test` or the site build, soft-fails `validate:schemas` via a stale v3.6-era echo fallback, and falls back from `npm ci` to `npm install`; the root test glob misses `packages/*` suites (admin's 44) | WS-D (D5) + WS-A (A5) |

Instance field state (for WS-B/WS-H): `refi-bcn-os` (prod, fw 3.0, branch `luizfernando`, ledger TF-1..6) · `refi-dao-os` (fw 3.0, 160 dirty, ledger A1..F1 incl. 🔴 B5 kms title-slug data loss, F1 dispatch package unsent) · `regen-coordination-os` (fw 3.0, still named `organizational-os-template`, dup `initialize` key) · `refi-med-os` (pristine 3.0 scaffold — ideal acceptance target; ⚠️ its `upstream` remote points at `organizational-os-framework.git`, a **live divergent legacy repo** — `git ls-remote` HEAD ≠ `org-os-template` HEAD, verified 2026-08-28) · `bread-coop-os` (fw 3.5, only genesis stamp, **no git remote**) · `dao-os` (fw 3.0, dormant since March). **Canonical-repo hazard:** three upstream names circulate — `org-os-template` (real origin), `organizational-os-framework` (live + divergent; refi-med's upstream), `org-os-framework` (the AGENTS.md §11 link) — B4/B8 detect + rewrite per instance; G4 reconciles the repos themselves.

---

## 1. Definition of done

1. `main` contains admin M1; full gate suite green (`npm test && npm run validate:schemas && npm run validate:structure && npm run version:check && npm run selftest && (cd site && npm run build && npm test)`). CI enforces the same suite: `validate.yml` runs `npm test` + site build/tests with no soft-fail steps (WS-D D5).
2. `packages/instance-doctor/` ships: the B1–B7 assessment battery (release-gating minimum per the WS-B preamble; incl. the wrong-URL-upstream + machinery-skew checks) + guided update/sync, TDD'd with **test files under root `tests/`** (inside the `npm test` glob — a suite under the package would be invisible to every gate; the multica-bridge trap), wired as `npm run doctor`, with `skills/instance-doctor/SKILL.md`.
3. Doctor acceptance passes against **refi-med-os** (pristine), **bread-coop-os** (genesis-stamped, remote-less) and **regen-coordination-os** (messy real instance): assess → sync from the release-candidate framework `main` → re-assess green, receipts written, lineage stamped — completed **before** the `v0.5.0` tag is cut (WS-H gates WS-G).
4. One versioning story: `v0.5.0` tag pushed; CHANGELOG `[0.5.0]` folded (incl. symbient, doctor); VERSIONING.md 0.x policy; MASTERPLAN.md aligned; root `VERSION.md` re-told to 0.5.0 and `update-version.mjs --check` extended to read it plus the `MASTERPLAN.md` header (all five surfaces machine-checked — C5); `version:check` green.
5. Site live at `https://regen-coordination.github.io/org-os-template/` (200s on all routes, `map.json` fetch works), auto-deploy on push to main, repo homepage + README link set.
6. Branch topology after release: `main` + frozen-work archive tags only; zero worktrees; nothing unreachable (`git rev-list` proof per deletion recorded in the triage report).
7. Every plan/spec/queue entry carries its v0.5 verdict + pointer here (WS-F stamping pass — mechanical batch delegated 2026-08-28).
8. DECISIONS.md records: memo adoption + doctor amendment, deploy target, versioning story, branch-clearing policy, kms data-loss disposition (ships as documented Known Issues; fix targets `v0.5.1` and gates v0.6 Active-1 propagation).

Explicitly **not** in v0.5 (v0.6 floor = external pilot gate, per memo §1): module engine beyond manifest-first, admin M2/M3, cloudflare-os deploy half, dfos, rad-org-os/kms-connector/tech-tree/knowledge-commons merges, wizard non-interactive mode (B2) + BOOTSTRAP reconciliation (B1) — *except* the two cheap doc honesty fixes (M1, M3) which ship in WS-F.

---

## 2. Workstreams

Order: WS-A → WS-B (longest, start early) → WS-C → WS-D → WS-E → WS-F → **WS-H → WS-G** (acceptance runs on the release candidate; the tag is cut only after WS-H passes). WS-C/D/E/F are parallelizable after WS-A lands. Vault rules bind every task: snapshot before merges (`npm run vault:snapshot -- "<reason>"`), explicit-path staging only, never the word "clean" in any git command line (guard over-matches — use `-F` message files when needed; A6 fixes this false-positive class, the workaround applies only until it lands).

**Execution environment (binds every handoff):** sessions run in the vault checkout (`03 Libraries/org-os`) — the worktrees and several unpushed local branches exist nowhere else; a fresh clone cannot execute A2/A4 or WS-E. `main` is currently held by the `.claude/worktrees/v05-main` worktree, so `git switch main` **fails in the primary checkout** until A0 removes it. H1 then owns `main`; H2/H3 branch from `origin/main` without checking out `main` and land via PR/merge after H1. `feat/berd-agents` (== main, the primary checkout's current branch) stays fast-forwarded to main between sessions and is retired as WS-E's last act.

### WS-A — Land admin M1 (absorbs ship-and-validate Task 4)

- [x] A0. Unblock `main`: remove the redundant `.claude/worktrees/v05-main` worktree (it holds `main` — until it's gone, `git switch main` fails in every session; copy out any non-regenerable untracked files first, 08-21 precedent), then switch the primary checkout to `main`. *(Moved up from the WS-E table: it blocks all of H1–H6, not just topology cleanup.)*
- [ ] A1. Snapshot. Refresh `feat/admin-app`: merge `main` into it (272-commit refresh; conflicts expected in `package.json`, matrices; union rule — nothing dropped silently; JSON files are reconciled by hand, never textual union — duplicate keys parse silently last-wins, the exact defect doctor check B4 hunts in instances; parse + duplicate-key check on `package.json` before committing), run admin tests (44) + full gates.
- [ ] A2. Promote the stranded spec: copy `.worktrees/admin-app/docs/superpowers/specs/2026-07-23-admin-app-design.md` → `docs/superpowers/specs/`, commit (fixes 4 dangling references found in the audit).
- [ ] A3. Merge PR #1 into `main` (merge commit, no squash — preserves the 18-commit history), verify `gh pr view 1` → `MERGED`, gates green on main.
- [ ] A4. Remove `.worktrees/admin-app`, tag `archive/feat-admin-app`, delete branch local+origin.
- [ ] A5. Wire the 44 admin tests into the gates: root script `test:admin` (`npm test --prefix packages/admin`) + a selftest step and a `validate.yml` step (coordinates with D5). Precedent makes this mandatory, not optional: `test:multica-bridge` and `test:cloudflare-os-integration` live outside `npm test` and therefore outside every gate — package suites that aren't wired in never run again after merge day.
- [x] A6. Fix the vault guard's false-positive class (`scripts/guards/deny-destructive-git.mjs`): stop treating `-` and `/` as word boundaries around `clean`/`stash` — today the filename `clean-room-bootstrap-2026-08-21.md` in a commit pathspec, or a grep pattern beside `git-hooks`, blocks harmless commands (observed twice on 2026-08-28). Keep dashed invocations (`git-clean`, `git-stash`) and all wrapper/chained destructive forms blocked — the fail-closed design is untouched; add regression fixtures for both observed false positives and every destructive form. Retires the workaround all six handoffs teach; F3 then marks HEARTBEAT's guard-defect entries fixed.

### WS-B — `packages/instance-doctor/` (new; the release's reliability centerpiece)

One package, two verbs, runnable from inside an instance (`npm run doctor`) **or** from the framework/hub against a sibling (`npm run doctor -- --dir ../refi-med-os`) — the hub mode is what breaks the bootstrap deadlock (instances can't self-update without sync machinery they don't have).

**Release-gating minimum** (this is the longest workstream and the likeliest to run long — cut here, not silently): B2, B3, B4, B7 (assess) + B8–B11 (sync, skill, wiring, tests) must land for the tag. B1/B5/B6 may slip to `v0.5.1`, with the cut recorded in the PR. Nothing else may slip.

**Assess** (read-only, scorecard output, each check = one pure function + fixture tests; every check below maps to a defect the 2026-08-28 sweep actually found):
- [ ] B1. Identity coherence: `IDENTITY.md` ↔ `federation.yaml.identity` ↔ `package.json.name` ↔ `.well-known/dao.json.name` agree; template-leakage detection (upstream placeholder IDs in members/projects, `organizational-os-template` naming) — closes clean-room B5/B6/B7.
- [ ] B2. Lineage: `genesis_commit` present + 40-hex; `last_sync_commit` vs framework HEAD (staleness in commits); recognizes prose-only stamps (refi-bcn case) and reports "unstamped fork".
- [ ] B3. Version surfaces: `federation.yaml.framework_version` vs `package.json` vs `VERSION.md` vs CHANGELOG — cross-scheme aware (3.x ↔ 0.x via the re-baseline map; **map source of truth: the CHANGELOG `[0.5.0]` re-baseline paragraph** — B3 cites it and C1 later formalizes the same map; don't invent a second one, H2 runs before H5), flags contradictions (the 1.0.0-vs-3.0 stub bug).
- [ ] B4. Machinery integrity: every `package.json` script whose file is missing (bcn/regen/bread `sync:upstream`), no-op stubs (dao 178-byte case, detected by content heuristic), duplicate JSON keys (regen `initialize`), missing `upstream` git remote, **wrong-URL upstream** (refi-med's `upstream` → `organizational-os-framework.git`, live + divergent — compare against the canonical `regen-coordination/org-os-template`), **no git remote at all** (bread), missing migrations dir, and **machinery skew** (fingerprint injected `sync-upstream.mjs`/doctor copies vs framework HEAD — copy-decay is the defect class behind gap #2; make its recurrence visible instead of silent).
- [ ] B5. Structure + schemas: reuse `validate-structure.mjs`/`validate-identity.mjs` as libraries where importable; degrade to subprocess.
- [ ] B6. Freshness: last commit age, memory recency, uncommitted-change count, sync receipts present.
- [ ] B7. Report: `--json` + human scorecard (BLOCKER/WARN/OK per check), exit 1 on blockers.

**Update/sync** (the reliable path; wraps + hardens what exists):
- [ ] B8. `doctor sync` orchestration: snapshot (vault-aware, **implemented inside doctor** — target instances have no `vault-snapshot.mjs`; refi-med has no scripts at all) → ensure `upstream` remote **with the canonical URL** (add if missing, rewrite if stale/divergent — the refi-med case) → fetch → install/refresh sync machinery into the instance (`sync-upstream.mjs`, `validate-identity.mjs`, doctor itself — the bootstrap-injection step) → run `sync-upstream.mjs` (10-stage, already fixed + 17 tests) → `migrate` (must include the 3.x→0.5 re-baseline re-stamp of the instance's version surfaces — `federation.yaml.framework_version`, `VERSION.md`, `package.json` — else the final re-assess fails B3) → `generate:schemas` → re-assess → write dated receipt to `memory/reports/sync-receipt-YYYY-MM-DD.md` + stamp `last_sync_commit`.
- [ ] B9. Failure containment: every stage's failure aborts forward motion, never leaves half-state (receipt records the abort stage); `--dry-run` prints the full plan.
- [ ] B10. `skills/instance-doctor/SKILL.md` (operator flow: assess → read scorecard → sync → verify) + `npm run doctor` + registration in `data/packages-matrix.yaml` (lifecycle `active`), `data/skills-matrix.yaml`, `federation.yaml agent.skills`, SKILLS.md regen. **Plus `modules/org-os-instance-doctor/module.yaml`** (manifest-first, validated by the shipped `validateManifest()`, mirroring `org-os-cloudflare-os`) — the **second tracked module**, deliberately firing the module-engine un-freeze trigger for v0.6 per the interop/plugin directive ([`interop-plugin-architecture.md`](../../agent-plans/interop-plugin-architecture.md)).
- [ ] B11. Tests: fixtures modeled on the six real instances' failure signatures (each sweep finding above becomes a fixture); **test files under root `tests/`** so the `tests/**/*.test.mjs` glob actually runs them — a suite under `packages/instance-doctor/` would be invisible to `npm test`, selftest, and CI (the multica-bridge trap); suite green in `npm test`.

### WS-C — One versioning story (absorbs ship-and-validate Task 10)

- [ ] C1. `docs/VERSIONING.md`: add "The 0.x pre-beta line (current)" (re-baseline story, 0.minor = milestone, 1.0.0 reserved), demote 3.x to Historical.
- [ ] C2. CHANGELOG: fold `[Unreleased]` (symbient v2) + everything since 2026-06-17 into `[0.5.0]` dated ship day (milestone bullets: consolidation, federation map, autopoiesis P2, cloudflare-os M0–M2, admin M1, kms/quilt/tech-tree*, graphify, symbient, instance-doctor, site live, positioning truth-up, branch archive policy; *tech-tree noted as frozen-branch, not shipped). Reset stub. Add a **Known issues** subsection carrying the two 🔴 kms data-loss defects (per the F4 decision).
- [ ] C3. Root `MASTERPLAN.md`: version 2.0.0 → 0.5.0, date, replace the stale v2 "Activations" block with a pointer to this masterplan + memo Actives; fix the two v2-era instance-status table rows.
- [ ] C4. `npm run version:check` green; remove `MASTERPROMPT.md` if operator confirms (pre-existing warning; ask, don't assume).
- [ ] C5. Root `VERSION.md`: 1.0.0 → 0.5.0 (+ re-baseline note), and extend `scripts/update-version.mjs` (incl. `--check`) to also read root `VERSION.md` and the `MASTERPLAN.md` version header — today it checks only package.json/federation.yaml/CHANGELOG, which is exactly how a root file saying 1.0.0 survived a "version chaos" cleanup unnoticed. This is what makes DoD #4's "all five surfaces" machine-checked rather than hand-fixed once.

### WS-D — Site live (absorbs ship-and-validate Tasks 11–12; copy already wired)

- [ ] D1. `site/astro.config.mjs`: `site: "https://regen-coordination.github.io"`, `base: "/org-os-template"`. Add `site/src/lib/base.ts` (`withBase()` helper). Route every root-absolute `href/src/fetch` through it — known hits: landing CTA renderer, `<federation-map src="/map.json">`, nav/footer, `verify-build.mjs` expectations. Gate: built `dist/index.html` contains zero unprefixed internal links.
- [ ] D2. `.github/workflows/deploy-pages.yml`: push-to-main; if paths-filtered, the filter **must include `packages/org-os-kms/**` alongside `site/**`** (the site imports it — a narrow filter skips the deploy exactly when a cross-coupled package change breaks the site); **root `npm ci` before `site/ npm ci`** (site imports `../../packages/org-os-kms`); build must degrade gracefully with no sibling clones (aggregator already does; verify `buildMap` on cold cache).
- [ ] D3. Enable Pages (`gh api -X POST repos/regen-coordination/org-os-template/pages -f build_type=actions`; 409 → PUT), watch first run, verify live: all top routes 200, hero copy string present, `map.json` 200.
- [ ] D4. `gh repo edit --homepage`, README live-URL line (via `templates/README.framework.md` + `render:templates` if that's the source), commit.
- [ ] D5. `validate.yml` → full CI gate (closes gap #9 with A5): add `npm test`, remove the echo soft-fail on `validate:schemas`, drop the `npm ci || npm install` fallback, add the site build + tests. Verify one green run on a PR or push before WS-G. Without this, every "gates green" in the plan depends on session discipline alone — untenable with concurrent sessions.

### WS-E — Clear the branch/worktree topology (operator directive: all of it)

Policy: **push → tag `archive/<name>` → push tag → delete branch (local+remote) → remove worktree.** Nothing becomes unreachable; frozen work resumes from tags when memo triggers fire. Record every action + rev-list proof in `memory/reports/branch-triage-2026-08-28.md`.

| Branch (unmerged vs main) | Action |
|---|---|
| `release/v3.5-docs-prep` (45, contained in `archive/v3.5-execution`) | tag+delete, remove worktree `v3-5-docs-prep` — zero info loss (memo §5) |
| `release/v3.5-templates` (7, contained) | tag+delete, remove worktree `v3-5-templates` |
| `consolidation-backup` (0) | tag+delete |
| `agent/ORG-4` (2 doc commits) | **cherry-pick both** (session doc + idea note — cheap, real content), then tag+delete |
| `feat/knowledge-commons` (32, frozen) | push to origin first (backup), tag+delete |
| `feat/rad-org-os` (55, frozen) | push, tag+delete, remove worktree `rad-close` |
| `feature/kms-connector-layer` (19, frozen) | push, tag+delete, remove worktree `kms-connector-layer` |
| `feature/tech-tree` (21, frozen) | push, tag+delete, remove worktree `tech-tree` |
| `feat/admin-app` | WS-A4 (post-merge) |
| `.claude/worktrees/v05-main` | **moved up to A0** (it holds `main` and blocks every session's `git switch main`) — verify gone |
| `feat/berd-agents` (the primary checkout's current branch, == main) | **last act of WS-E:** switch the primary checkout to `main`, verify no concurrent session still sits on it, tag `archive/feat-berd-agents`, delete local+origin |
| origin-only stale refs (`origin/autopoiesis-phase2-pilot`, `origin/v0.5`, `origin/feat/multica-operator`, `origin/release/v3.5-design`) | delete remote refs (local archive tags already pushed) |

- [ ] E1. Execute the table (snapshot first; worktree removals may need `--force` for untracked regenerables — copy any non-regenerable file out first, per the 08-21 precedent).
- [ ] E2. Bounded salvage review (memo §6, pre-v0.6 exception): diff `archive/v3.5-execution`'s 983-line `clone-framework.mjs` + reliability tooling vs main's versions; cherry-pick only clear wins; timebox one session; report in the triage report.
- [ ] E3. `git worktree list` shows main only; `git branch` shows `main` only; final gates green.

### WS-F — Status convergence (the "everything references this masterplan" pass)

- [x] F1. ✅ *Delegated 2026-08-28:* mechanical stamping of 37 plans + 17 specs with verdict banners pointing here (landed as `bad8b8e` — verified 2026-08-28).
- [x] F2. ✅ *Done 2026-08-28, verified (branch entries, downstream-propagation, external-pilot, instance-doctor all present).* `QUEUE.md` full rewrite: Active = {this masterplan} + memo Active-3 as v0.6 successors; apply every freeze verdict + trigger; fix the false admin-app claims (plans are tracked, spec promoted in WS-A2), the triple-numbered entries, the resolved autopoiesis note; **add missing entries** (tech-tree, kms-connector-layer, knowledge-commons, rad-org-os branches; downstream-propagation; external-pilot; instance-doctor).
- [ ] F3. `HEARTBEAT.md`: mark done-but-unchecked items (autopoiesis P2, graphify scope Q, v3.0.0-tag superseded, changelog stub, federation-downstream-verified); re-date footer; add "v0.5 release in flight — masterplan" pointer at top; keep the clean-room fix-list section intact (deferred scope, correctly parked); mark the guard-defect false-positive entries fixed once A6 lands.
- [ ] F4. Register the two instance promotion ledgers as convergence inputs: new section in `docs/SKILL-PROMOTION.md` (or DATA-MODEL "Recognized Extension" note) indexing `refi-bcn-os` TF-1..6 + `refi-dao-os` A1..F1. **Decision, locked here (DECISIONS entry in WS-G): the two 🔴 kms data-loss items (B5 title-slug overwrite, D-provenance) ship in v0.5 as documented Known Issues (CHANGELOG `[0.5.0]` + release note + HEARTBEAT); the fix targets `v0.5.1` and hard-gates v0.6 Active-1 propagation** — no fleet sync while a known data-loss defect is open. Not "candidates": a reliability release with an undecided data-loss bug undercuts its own claim. Note F1 dispatch package re-targets `archive/knowledge-commons` tag (v0.6).
- [ ] F5. Cheap doc-honesty fixes from the clean-room list: **M1** (delete `docs/SETUP-PATHS.md` or reduce to an honest stub) + **M3** (README: one clear bootstrap recommendation, `clone:framework`, with `npm run setup` framed as in-place TTY-only alternative). B1/B2/M2/M4 stay deferred (first v0.6 items).
- [ ] F6. Untracked strays in `memory/reports/` (`instances-drift-2026-04-25/-08-20/-08-21/-08-28.md`): commit them, or discard **only with explicit operator ok** (untracked vault content). Untracked reports are invisible to clones and break the supersession chain — the latest *tracked* drift report is the 08-23 bot run. Rule going forward: the bot commits its own reports; local `analyze:instances` runs must commit or remove theirs same-day.

### WS-H — Prove it (acceptance; runs **before** WS-G — the tag is only cut after this passes)

- [ ] H1. Doctor acceptance on **refi-med-os** (pristine 3.0 scaffold): assess (expect: unstamped, missing machinery, stale fw, **wrong-URL upstream** — its remote points at the divergent legacy repo; B4 must flag it, B8 must rewrite it before fetch) → `doctor sync` from the release-candidate framework `main` → re-assess green → receipt + lineage stamped. This is the v2→v3→v0.5 cross-scheme migration test.
- [ ] H2. Doctor acceptance on **bread-coop-os** (genesis-stamped, no remote): doctor must flag the missing remote as BLOCKER with a remediation hint, then sync locally from the sibling framework checkout.
- [ ] H3. Doctor acceptance on **regen-coordination-os** (the messy case: still named `organizational-os-template`, dup `initialize` key, missing sync machinery): assess → snapshot → `doctor sync` → re-assess green → receipt + lineage stamped. Greenfield proof alone doesn't carry the reliability claim; a degraded real instance does.
- [ ] H4. Assess-only run on the three remaining instances (**refi-bcn-os** prod · **refi-dao-os** 160 dirty + the open 🔴 kms defects · **dao-os** dormant) — assess-only is deliberate: prod risk and the F4 known issues keep them out of sync scope until v0.5.1/v0.6. **Plus `doctor sync --dry-run` against refi-bcn-os and refi-dao-os** — read-only, mutates nothing, and proves sync *plan generation* survives dirty production trees before v0.6 bets on them. Publish `memory/reports/instances-drift-2026-08-28.md` from the results (supersedes the tracked 08-23 bot report; the untracked 08-20/08-21 strays are handled in F6 — the 08-28 file may already exist from local `analyze:instances` runs; overwriting the auto-generated report is fine). **Full propagation of all instances = v0.6 Active-1**, gated on the F4 kms fix.
- [ ] H5. Carry acceptance results into WS-G's release note + CHANGELOG `[0.5.0]` amendments if needed.

### WS-G — Ship (runs last, only after WS-H is green)

- [ ] G1. Full gate suite on main + green `validate.yml` CI run (post-D5 it enforces the same suite); `npm run vault:snapshot -- "v0.5.0 release point"`.
- [ ] G2. DECISIONS.md: five entries (memo adoption + doctor amendment · deploy target · one-versioning-story · branch-clearing policy · kms data-loss disposition per F4). **Fetch + fast-forward `main` immediately before tagging** — the weekly drift bot commits straight to main and has already raced this release once (merge `6930b0b`); consider pausing the cron for ship week. Then tag `v0.5.0` (annotated), push `main --follow-tags`; push historical tags as `archive/v3.0.0`/`archive/v3.5.0` — never bare `v3.x`, which would outrank `v0.5.0` in semver-sorted tag lists and undo the one-versioning-story at the surface newcomers see.
- [ ] G3. `memory/2026-08-28.md` release note incl. WS-H acceptance results; `npm run vault:audit`.
- [ ] G4. Canonical-repo sweep (**operator approval required** — `AGENTS.md` is a protected core file): fix the AGENTS.md §11 upstream link (`org-os-framework` → `org-os-template`); draft-and-present the fate of the live, *divergent* `organizational-os-framework` GitHub repo (recommend: archive it + pointer README to `org-os-template`). One framework with three URLs in circulation is an adoption hazard the doctor can only patch per-instance.

### WS-I — Adoption & Session Kit (added 2026-08-28, operator directive after the 260827 Regen Knowledge Commons Sync)

**Context:** Luiz leads the "Repo Stabilization" Linear project (org-os side) for the Regen Knowledge Commons group; an **org-os session for that group is anchored ~2026-09-10** (`task-260827-luiz-orgos-session-scheduling`), with DAIAA flagged as the first external audience and the group's agreed experiment being a lightweight org-os instance **on a branch off the `knowledge-commons` repo, never `main`** (`task-260827-luiz-orgos-branch`). Meeting record: `../regen-coordination-os/repos/regen-toolkit/packages/operations/meetings/260827 Regen Knowledge Commons Sync.md`. This workstream produces the polished understand/setup/adopt artifacts for that session and wider adoption. **The `v0.5.0` tag does not wait on WS-I** — the session rides on the tagged release; WS-I's own gate is the session date.

**Scope consequence (amends §3):** the clean-room doc fixes B1, M2, M4 move **into** this release (they are load-bearing for a session whose attendees will attempt setup); B2 is satisfied **by documentation** — the recommended agent-driven path is `clone:framework`'s existing non-interactive config mode, not a new wizard mode. The fork-target/blank-template decision (B3–B6) stays deferred: the recommended path strips framework identity by construction, sidestepping the leak.

- [ ] I1. **One honest setup path.** Rewrite the quickstart trio around `npm run clone:framework` as the single recommended path: `templates/GETTING-STARTED.md`, README quickstart (via `templates/README.framework.md` + `render:templates`), and `BOOTSTRAP.md` reconciled to what the tools actually ask (closes B1); `docs/OPERATOR-GUIDE.md` Level 2 made honest (M2); `clone-framework.mjs` "Recommended" framing reconciled with the docs (M4). `npm run setup` is framed as the in-place TTY-only alternative with its real nine prompts listed.
- [ ] I2. **Agent-driven setup recipe.** A short doc (`docs/ADOPT-WITH-AN-AGENT.md` or a GETTING-STARTED section): driving `clone:framework` config mode from Claude Code / Cursor / ChatGPT-connector setups — the audience's actual tools per the 260827 tooling walkthrough. Verify by executing the recipe once in a scratch dir; patch code only if the recipe breaks.
- [ ] I3. **Website adoption surfaces** (after WS-D lands): `/get-started` page truth-up to the I1 path; docs allowlist curated for the adoption journey (concepts from POSITIONING §3 → setup → federation); numbers box re-verified against the tagged release; single-operator disclosure kept intact (F1 honesty is a feature in this room, not a bug).
- [ ] I4. **Session kit.** Session narrative + live demo script (dashboard `/initialize` → federation map → admin M1 → `doctor assess` on a real instance → site tour), a rendered one-pager (`templates/render.mjs`), and a short FAQ from POSITIONING §7 (incl. the "why not X" comparisons and the Durgadas-overlap talking point for the 1:1 — `task-260827-luiz-durgadas-1to1`).
- [ ] I5. **Live demo instance:** execute `task-260827-luiz-orgos-branch` — lightweight org-os (knowledge-management module) on a branch off `knowledge-commons`, `main` kept clean per Monty's condition; this is both the session's live proof and the first semi-external adoption datapoint. Value evaluation before any merge proposal.
- [ ] I6. **Clean-room re-run (WS-I acceptance).** Repeat the Harbor Bakery protocol (task-14 brief) against the polished kit: fresh clone, docs-only, agent-driven. Gate: a valid instance, `doctor assess` green, zero silent identity leaks. Report to `memory/reports/`.
- [ ] I7. **Schedule + follow through:** propose session times (~2026-09-10+) to the group; post-session, recruit the external pilot (Active-2) from attendees/DAIAA.

---

## 3. Deferred registry (v0.6+, all preserved, all triggered)

Everything in the portfolio memo freeze table (§4 rows 0–13) + phase-3 autopoiesis + admin M2/M3 + the wizard fork-target/blank-template split (B3–B6; B1/M2/M4 moved into WS-I, B2 satisfied by the documented `clone:framework` config path) + module engine + dfos + full ledger fold (TF/A–F items beyond the 🔴 pair) + custom domain + git-timestamp investigation + hub-local scheduled `doctor assess` (the GH drift bot runs in a bare clone and cannot see sibling instances — a hub-side cron is the standing fleet monitor) + npm-dependency distribution of doctor/sync machinery (replaces copy-injection; retires the copy-decay defect class that B4's skew check can only detect). Un-freeze floor: the external-pilot gate (DECISIONS 2026-08-21). Frozen branch work lives in `archive/*` tags — resuming = `git switch -c <name> archive/<name>`.

## 4. Subplan index (verdict → pointer)

Ships in v0.5: `github-pages-deploy` (decisions locked) · `org-os-website` (deploy half) · admin-app M1 · ship-and-validate remainder (absorbed) · this masterplan.
Completed (stamped): federation-map · generate-quilt · graphify-kms · autopoiesis P1+P2 · symbient v2 · cloudflare-os m0-m2 (in-repo half) · module-v05-docs (substantially) · v2-phase1 · versioning-system (policy superseded).
Deferred (stamped, triggered): autopoiesis P3 · admin M2/M3 · cloudflare m3-m4 + deploy · v5-module-system P1 · kms-connector-layer · tech-tree · org-os-kms/knowledge-commons · rad-org-os-artifacts · skills-section · multica ×2 · dfos · tui-dashboard ×2 · instance-bootstrap · package-integration · system-reliability · federation-protocol · future-instance-specs · non-tech-onboarding · obsidian ×2 · framework-dashboard-template · commands-consolidation · philosophy-manifesto.
Superseded: v3-5-release-implementation · 2026-08-10-v0.5-beta-ship (+spec) · multica-org-os-operator · obsidian-terminal-setup (out of scope).
