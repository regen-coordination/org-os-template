---
id: v0.5-release
title: "org-os v0.5 — Trunk Repair, Identity Reset, Release"
status: active
priority: 1
scope: framework
depends_on: []
created: 2026-07-23
started: 2026-07-23
completed: null
estimated_sessions: 4-6
tags: [v0.5, release, integration, identity]
---

## Goal

Ship org-os v0.5: repair the fragmented trunk (~230 unmerged commits across 6 branches), strip refi-bcn-os identity contamination from the framework, fix the dashboard, get all validators green, and align docs with reality. **v0.5 is an intentional renumber** — resetting the incoherent version story (CHANGELOG 1.0.0 / MASTERPLAN 2.0.0 / federation manifest v3.0 / unmerged "v3.5.0" branch) to a pre-1.0 line that honestly reflects framework maturity.

## Current State (audit 2026-07-23)

Full review via graphify (3,037 nodes / 4,818 edges / 204 communities) + two audit agents:

- **Branch fragmentation**: `origin/main` +1 (fixes 6 broken scripts); `release/v3.5-execution` +54 (v3.5.0 closeout, 12/14 gates, stale since Apr 25; contains docs-prep + templates); shared feature line +87 → `feature/kms-connector-layer` +39, `feature/tech-tree` +21; `feat/knowledge-commons` (HEAD) +3.
- **Contamination**: `package.json` name `refi-bcn-os`; `.well-known/dao.json` real ReFi BCN data; hardcodes in `setup-org-os.mjs:423`, `generate-all-schemas.mjs:49`, `dashboard.yaml:1`, `build:site`/`serve:site`.
- **Dashboard**: renders "Organization · none" + template tasks because `loadIdentity` reads placeholder `federation.yaml`/`HEARTBEAT.md`; emoji-width padding bug at `initialize.mjs:1683` + `pad()` at `:1198`.
- **Broken scripts** (10 targets): 6 fixed by origin/main `5005fe4`; remaining: `quartz`, `setup:cursor`, `sync:upstream`, `build:site`/`serve:site`, no `tsconfig.json`.
- **Doc drift**: CLAUDE.md "9 skills" (real: 12), "13 registries" (real: 14); `federation.yaml` lists 6/12 skills; README broken links (`OPERATOR-GUIDEBOOK.md`, vault-relative paths, pnpm); `.opencode/command/`+`commands/` duplicates; two diverged `/close` protocols; missing `MASTERPROMPT.md` ref; `docs/plans/QUEUE.md` phantom path in `dashboard.yaml:58` + `.claude/commands/close.md`.
- **Schema gap**: 7 of 14 registries have no `.well-known` surface (governance, funding-opportunities, relationships, events, channels, assets, knowledge-gaps).

## Tasks

### Phase 0 — Version decision ✅
- [x] Decide version: **v0.5 intentional renumber** (operator, 2026-07-23). Record rationale in VERSION.md + CHANGELOG at release.

### Phase 1 — Repair the trunk
- [x] 1.1 Fast-forward `main` to `origin/main` (`5005fe4` — restores validate-identity, compile-knowledge, lint-knowledge, index-linked-repos, update-knowledge-index, dashboard pkg).
- [ ] 1.2 Audit + merge `release/v3.5-execution` into `main`; re-run its 14 acceptance gates post-merge.
- [ ] 1.3 Merge feature line: shared base, then `feature/kms-connector-layer`, then `feature/tech-tree`.
- [ ] 1.4 Rebase `feat/knowledge-commons` onto repaired `main`; delete merged branches + prune worktrees.
- [ ] 1.5 Hygiene: commit regenerated `.well-known/*.json` (+ new `meetings.json`); gitignore `.worktrees/`, `site/`, `__pycache__/`; delete `.gitignore.test`, `packages/hermes-integration/`; review untracked `packages/org-os-federation-map/`.

### Phase 2 — De-contaminate framework identity
- [ ] 2.1 `package.json` → name `org-os`, framework description; drop `build:site`/`serve:site` instance scripts.
- [ ] 2.2 Regenerate `.well-known/dao.json` from clean template.
- [ ] 2.3 Strip hardcodes: `setup-org-os.mjs:423`, `generate-all-schemas.mjs:49`, `dashboard.yaml:1`, `.env.example`.
- [ ] 2.4 Give framework a real self-identity: fill `federation.yaml` identity ("org-os Framework"), real `HEARTBEAT.md` tasks.

### Phase 3 — Fix the dashboard
- [ ] 3.1 Display-width-aware `pad()` (`initialize.mjs:1198`) + widen app-name column (`:1683`).
- [ ] 3.2 Fix `docs/plans/QUEUE.md` → `docs/agent-plans/QUEUE.md` in `dashboard.yaml:58` and `.claude/commands/close.md` (graph flagged both AMBIGUOUS).

### Phase 4 — Green checks (release gate)
- [ ] 4.1 Restore-or-remove remaining dead scripts: `quartz`, `setup:cursor`, `sync:upstream`.
- [ ] 4.2 Add `tsconfig.json` (or drop tsc from `check`); `prettier --write` offenders.
- [ ] 4.3 Gate: `npm run check` + `validate:schemas` + `validate:structure` all green.

### Phase 5 — Docs/skills truth alignment
- [ ] 5.1 CLAUDE.md: 12 skills, 14 registries, remove/restore missing-script refs.
- [ ] 5.2 `federation.yaml` agent.skills: list all 12.
- [ ] 5.3 README: fix `OPERATOR-GUIDEBOOK.md`, vault-relative links, pnpm→npm.
- [ ] 5.4 Dedupe `.opencode/command/` vs `commands/`; reconcile the two diverged `/close` protocols into one canonical; fix `MASTERPROMPT.md` ref in `.opencode/opencode.json`.
- [ ] 5.5 DATA-MODEL.md: remove `sync:notion` phantom ref.

### Phase 6 — Schema surface completion
- [ ] 6.1 Decide which of the 7 uncovered registries get `.well-known` surfaces; extend `generate-all-schemas.mjs`.

### Phase 7 — Knowledge layer
- [ ] 7.1 Finish KMS per `docs/superpowers/specs/2026-07-23-org-os-kms-design.md` (current `feat/knowledge-commons` line).
- [ ] 7.2 Queue items: graphify-knowledge-pages, graphify-knowledge-lint.
- [ ] 7.3 Triage 268 open entries in `data/knowledge-gaps.yaml`.

### Release
- [ ] CHANGELOG `[0.5.0]` entry + VERSION.md renumber rationale; bump `package.json` version `0.5.0`; tag `v0.5.0`.

## Notes

- Vault-safe protocol applies: `npm run vault:snapshot` (parent vault) before every merge/rebase step.
- `.worktrees/kms-connector-layer` and `.worktrees/tech-tree` are live worktrees — never delete before their branches merge.
