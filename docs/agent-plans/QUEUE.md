# Plan Queue — org-os (framework)

> Last updated: 2026-08-10 (cloudflare-os module framing + v0.5 docs spec approved; two plans written)

## Active

0. **cloudflare-os-integration** — **M0–M2 built and locally verified; two plans ready 2026-08-10.** The Cloudflare OS bridge: `gatekeeper-org-os` over a swappable substrate interface, a pure page core shared with `page-shim`, and the `org-dashboard` gadget. 86 tests green; verified against the live GitHub API for both the public hub and the private `refi-bcn-os` on a local Cloudflare OS stack. Everything still open is either operator work on a Cloudflare account (the runbook in [`docs/integrations/cloudflare-os.md`](../integrations/cloudflare-os.md)) or the two plans below. Design: [`2026-08-08-cloudflare-os-org-os-integration-design.md`](../superpowers/specs/2026-08-08-cloudflare-os-org-os-integration-design.md) · workstream: operator-interfaces.
   - **Plan A — module + v0.5 docs** (10 tasks): [`2026-08-10-cloudflare-os-module-v05-docs.md`](../superpowers/plans/2026-08-10-cloudflare-os-module-v05-docs.md), spec [`2026-08-10-cloudflare-os-module-v05-docs-design.md`](../superpowers/specs/2026-08-10-cloudflare-os-module-v05-docs-design.md). Makes the integration org-os's **first tracked module** (`modules/org-os-cloudflare-os/module.yaml`, validated by the engine's shipped `validateManifest()`), gives the framework a four-layer self-description (README template spine + `docs/MODULES.md` catalog, deprecating `PACKAGES.md`), wires the canonical chain into the site under a drift test, and declares the shipped `Substrate` interface as the rad-org-os driver seam. **Absorbs** the open `org-os-website` positioning-wiring task.
   - **Plan B — M3–M4** (13 tasks): [`2026-08-10-cloudflare-os-m3-m4.md`](../superpowers/plans/2026-08-10-cloudflare-os-m3-m4.md). The non-tech **write path** — four PR-only capabilities (idea, meeting, project status, action item) over comment-preserving text edits, idempotent by payload hash, trust-gated — plus the `org-inbox` gadget, `get_federation_map` + the federation-map gadget, and `selftest` wiring. One discovery gate (the upstream approval-queue action API) blocks only the adapter task.

1. **admin-app** — **Design-approved + M1 BUILT 2026-08-02.** The framework's first read-**write** web surface: `packages/admin/` (Hono API + Vite/React SPA) editing an instance's 14 `data/*.yaml` registries through schema-driven forms, committing every change to git. Architecture: backend-API + SPA, local-first v1 (localhost, no auth) → hosted v2 unchanged; **layered proposals** (a `data/proposals.yaml` entry ↔ a `proposal/<slug>` git branch); editing **rings** (v1 registries → B markdown → C system files, C always via proposal); **A+B shell** (Map canvas + Overview dashboard as co-equal homes, canvas = registry graph + graphify expansion); views built natively over registries (Anytype: integrate/adapt, licence + architecture rule out forking — see `DECISIONS.md` 2026-08-02). **M1 complete** on `feat/admin-app` (18 commits, **PR [#1](https://github.com/regen-coordination/org-os-template/pull/1)** open → `main`): comment-preserving YAML CST writes, 15 JSON Schemas, Ajv + referential validation, vault-safe git service, per-registry write lock, WS live updates; 44 tests green, HTTP e2e verified, final review's 3 defects fixed. Spec: [`2026-07-23-admin-app-design.md`](../superpowers/specs/2026-07-23-admin-app-design.md) · plans `docs/superpowers/plans/2026-07-23-admin-app-{m1,m2,m3}.md` (**on disk only — that dir is gitignored**) · workstream: operator-interfaces. **Next:** merge PR #1, then M2 (Map + view engine + ⌘K, 10 tasks) → M3 (proposals engine, worktree-backed branches, registry-level diff, transactional ratify, real `.well-known/proposals.json`, 10 tasks).

2. **org-os-website** — **Design-approved 2026-06-17** (brainstorm + visual companion). org-os v0.5 public framework website + docs site at `org-os/site/` (Astro 5, static). Presents the framework + v0.5 module roadmap, renders curated docs from `../docs` (single source of truth, content-collection `glob()` loader), and federates live-at-build from `data/instances.yaml` + sibling instance `.well-known/`+`data/`. Light "systems" theme (Geist Mono, hairline grid, federation graph) in a token/theme-swap architecture — the reusable core is the seam toward `org-os-website-generator`. Spec: [`2026-06-17-org-os-website-design.md`](../superpowers/specs/2026-06-17-org-os-website-design.md) · 15-task plan: [`2026-06-17-org-os-website.md`](../superpowers/plans/2026-06-17-org-os-website.md) · workstream: public-surfaces · **BUILT 2026-06-17** on branch `feat/org-os-website` (static Astro site at `site/`; `npm run build` green, 6/6 aggregator tests). Remaining before merge/deploy: final comprehensive review, then lock open decisions (domain, accent, final docs allowlist — spec §16). **Positioning copy ready 2026-08-02**: [`docs/POSITIONING.md`](../POSITIONING.md) supplies hero/about/features/modules/use-cases/comparison copy grounded in the verified four-layer thesis ([`landscape research`](../research/2026-07-15-agent-native-org-landscape.md), `DECISIONS.md` 2026-08-02) — `site/src/data/landing.yaml` + `modules.yaml` still carry pre-positioning copy and need wiring

3. **autopoiesis-research** — **Phase 2 complete 2026-08-02, gate passed.** Phase 1 (2026-05-02) selected Loop C at the gate (Population learning — Metabolism → Cognition → Federation, cascade closure), overriding spec default Loop A. Closing edge: `scripts/sync-upstream.mjs` + `scripts/validate-identity.mjs` + lineage stamp in `federation.yaml.metadata` — all three implemented (`ec09cdc`) and, in the Phase 2 pilot, tested + fixed + exercised end-to-end on branch `autopoiesis-phase2-pilot` (9 commits, `5b21144..b0b5bf3`). **The loop closes:** a synthetic instance pulled a framework bump, preserved its customization through rebase, pinned `last_sync_commit` to upstream HEAD, and wrote a dated receipt. Test harness: `tests/scripts/{sync-upstream,validate-identity}.test.mjs` (17 cases, all green; suite 65/65). The tests caught a **fatal shipped bug** — a stage-5 null-handling crash meant `sync-upstream` misreported every successful pull as a failure, so as shipped it could never complete a real sync; **this fix is load-bearing for downstream migration (the broken version cannot sync itself)**. Also added: `genesis_commit` auto-seeding (stage 9), `memory/` mkdir guard (stage 10), validate-structure §8b, FEDERATION.md lineage docs. Postmortem + 6 Phase 3 decisions + per-instance migration note: [`PILOT-framework.md`](../superpowers/research/2026-05-02-autopoiesis/PILOT-framework.md). Synthesis: [`SYNTHESIS.md`](../superpowers/research/2026-05-02-autopoiesis/SYNTHESIS.md). Phase 2 plan: [`2026-05-02-autopoiesis-phase2-pilot.md`](../superpowers/plans/2026-05-02-autopoiesis-phase2-pilot.md). Spec: [`2026-05-02-org-os-autopoiesis-design.md`](../superpowers/specs/2026-05-02-org-os-autopoiesis-design.md) · workstream: framework-evolution. **Next:** Phase 3 cascade — [`2026-05-02-autopoiesis-phase3-decisions.md`](../superpowers/plans/2026-05-02-autopoiesis-phase3-decisions.md); pick the merge path for `autopoiesis-phase2-pilot` first (branch also carries interleaved v5-modules/admin-app commits from a concurrent session, and the plan's original target `release/v3.5-design` is stale vs. the live trunk `feat/multica-operator`)

## Queued
1. [github-pages-deploy](github-pages-deploy.md) — **Deploy the built org-os website (static Astro `site/`, from `feat/org-os-website`) to a live public URL via GitHub Pages.** Verified 2026-08-02: no live site exists anywhere (`org-os.dev` NXDOMAIN; no Pages on any repo). Adds `.github/workflows/deploy-pages.yml` + enables Pages (Source: Actions). Open decisions to lock first: target repo (`org-os-framework` recommended), URL strategy (github.io project path vs custom domain — needs `base` set), and which `site/` branch is canonical. Fast path → `regen-coordination.github.io/org-os-framework/` in one session · depends_on: `org-os-website` · workstream: public-surfaces
2. **skills-section** — Dashboard Skills section + `/skills` command + generated `SKILLS.md` + `.well-known/skills.json` so operators can verify skills are loaded across workspace, user, and plugin sources. Spec: [`2026-04-27-skills-section-design.md`](../superpowers/specs/2026-04-27-skills-section-design.md) · 21-task plan: [`2026-04-27-skills-section.md`](../superpowers/plans/2026-04-27-skills-section.md) · workstream: operator-interfaces · TDD-driven, low-risk
2. **aggregator-package-promotion** — Phase 6 of the regen-coordination-os aggregator initiative — promote shared `aggregator-{ui,data,config}` packages to `packages/` and the `apps/regen-ecosystem-starter/` template to `templates/aggregator-starter/` after v1 ships. Also: registered chosen agent skills (oklch-skill, frontend-design, webapp-testing, brand-guidelines, deploy-to-vercel) get verdicts (promote/improve/drop) and the `skills-curator` skill's default sources get updated (remove agentskills.io — confirmed not a marketplace; add discovered gaps as candidates for new skills to author: shadcn-installer, astro-bootstrap, dedicated a11y-audit, Zod-skill, data-adapters). Triggered post-launch from regen-coord-side spec: [`regen-coordination-os/docs/superpowers/specs/2026-05-03-aggregator-design.md`](../../../regen-coordination-os/docs/superpowers/specs/2026-05-03-aggregator-design.md) · workstream: package-integration + framework-evolution · execution deferred until aggregator v1 launches (~6–8 weeks out)
2. **multica-integration** — Wire Multica (github.com/multica-ai/multica) as primary agent runtime; self-hosted server, slash commands, write-only HEARTBEAT bridge. Spec: [`2026-04-25-multica-integration-design.md`](../superpowers/specs/2026-04-25-multica-integration-design.md) · 25-task plan: [`2026-04-25-multica-integration.md`](../superpowers/plans/2026-04-25-multica-integration.md) · pilot for `package-integration` Phase 3 · workstream: package-integration · execution deferred 2026-04-25
2. [tui-dashboard](tui-dashboard.md) — TUI dashboard (Ink) + agent-rendered pages, shared data layer · spec + 28-task implementation plan ([`tui-dashboard-implementation.md`](tui-dashboard-implementation.md)) ready · candidate packages `packages/tui-data/`, `packages/tui/` · workstream: operator-interfaces
3. [future-instance-specs](future-instance-specs.md) — Write specs for regen-coordination-os and regen-toolkit  ·  workstream: framework-evolution
4. [federation-protocol](federation-protocol.md) — End-to-end federation exchange testing and docs  ·  workstream: federation-protocol
5. [package-integration](package-integration.md) — Audit packages/, define lifecycle (`docs/PACKAGE-LIFECYCLE.md`), resolve consumption mechanism  ·  workstream: package-integration  ·  est. 3 sessions  ·  parent of multica-integration
6. [system-reliability](system-reliability.md) — Audit reliability infra, decide trigger layering (pre-commit / CI / scheduled), implement enforcement  ·  workstream: reliability  ·  est. 3 sessions
7. [instance-bootstrap](instance-bootstrap.md) — Framework cloning + wizard with package/skill selection + knowledge bootstrap (one source). Engine for `non-tech-onboarding` web wrapper  ·  workstream: instance-bootstrap  ·  est. 4 sessions
8. **dfos-integration** — **Design-approved 2026-08-01.** DFOS protocol (protocol.dfos.com, Metalabel) as the federation's cryptographic identity + verifiable-authorship layer: `did:dfos` for org instances **and** agents (scoped, revocable agent credentials), `packages/dfos-bridge/` wrapping the official `dfos` CLI + `dfos@metalabel` Claude Code skill, anchoring federation manifests / knowledge-commons artifacts / governance decisions (with witness countersignatures from peer orgs), `.well-known/dfos.json` + `federation.yaml` v3.1 + `npm run verify:federation`. Git stays canonical — DFOS adds proofs, not storage. Hosted relay first, self-hosted later; spaces product is a research-gated tail (no public API found). Phases 0–1 (~1.5 days) ship standalone value. Spec: [`2026-07-25-dfos-org-os-integration-design.md`](../superpowers/specs/2026-07-25-dfos-org-os-integration-design.md) · implementation plan pending (writing-plans next) · workstream: federation-protocol
9. **graphify-knowledge-pages** — `compile:knowledge` gains a graph source: community summaries → `knowledge/` page stubs via `graphify export --wiki`. Needs content-authority design (generated stubs must not drown curated pages). Depends on: graphify integration (shipped).
10. **graphify-knowledge-lint** — `lint:knowledge` cross-checks `knowledge/INDEX.md` against graph reality (orphaned pages, undocumented god nodes). Depends on: graphify-knowledge-pages.
11. **validate-structure-v3-audit** — audit remaining validate:structure checks against the v3.0 flat manifest format (the `federation section` drift fixed during graphify work may not be the only one).
12. **repo-check-health** — `npm run check` is currently broken independent of graphify: no `tsconfig.json` (so `tsc --noEmit` exits 1) and ~22 files fail `prettier --check`. Add a tsconfig or drop the tsc clause, and run `prettier --write` across the pre-existing offenders so `npm run check` can pass cleanly.

## Scoping

- **commands-consolidation** — Document and consolidate `.claude/commands/`: inventory across instances, decide canonical set, build a `/commands` listing surface (dashboard section + `COMMANDS.md` + slash command), mirror the skills-section pattern · workstream: operator-interfaces · follow-up to `skills-section` · **largely advanced 2026-07-15**: inventory done (cross-instance audit), canonical set now 6 (`close initialize skills commit sync handoff`), `sync-commands.mjs` mirroring mechanism promoted from refi-bcn-os — remaining scope is the `/commands` listing surface
- [non-tech-onboarding](non-tech-onboarding.md) — Web wizard bootstrap + GitHub Actions backend  ·  workstream: non-tech-onboarding  ·  depends on `instance-bootstrap`
- [framework-dashboard-template](framework-dashboard-template.md) — Reusable dashboard package template  ·  workstream: v2-stabilization  ·  becomes thin renderer over `tui-data` after `tui-dashboard` lands
- [obsidian-interface](obsidian-interface.md) — Obsidian as primary operator interface (candidate package `packages/obsidian-interface/`)  ·  workstream: operator-interfaces
- [obsidian-canvas-interface](obsidian-canvas-interface.md) — Obsidian Canvas as system overview + operational interface (candidate package `packages/obsidian-canvas/`)  ·  workstream: operator-interfaces  ·  depends on `obsidian-interface`

## Completed

- ~~**federation-map** ("the torch")~~ — Interactive map of an instance's external world: ring 1 federated instances · ring 2 frontier peers-of-peers · ring 3 sources/ecosystems; the counterpart to the internal note graph (two linked views joined by portals). New package `@org-os/federation-map` (framework-agnostic web component, d3-force sole dep) + kms data plane (`render map`, `federate frontier`, `render map html`). Surfaces: site `/federation` + home mini, and a self-contained offline vault artifact for Obsidian. Spec: [`2026-07-19-federation-map-design.md`](../superpowers/specs/2026-07-19-federation-map-design.md) · 14-task plan: [`2026-07-19-federation-map.md`](../superpowers/plans/2026-07-19-federation-map.md) · workstream: federation-protocol + public-surfaces · **completed 2026-08-02** (19+65+7 tests green; final review caught + fixed a Critical panel XSS on remote frontier data). Open follow-up: automated bundle-drift test.
- ~~[v2-phase1-framework](v2-phase1-framework.md)~~ — Framework standards, docs, skills, data model, session lifecycle
- ~~[versioning-system](versioning-system.md)~~ — Reconciled versioning + migrations + changelog + policy · workstream: v2-stabilization · completed 2026-04-24
