---
description: Show the org-os decisions log (DECISIONS.md)
agent: build
---

!`npm run page --silent -- decisions`

If the operator asks about a specific decision, run `npm run page --silent -- decision/<slug>` (once entity pages are supported by the renderer).
