---
description: Show the org-os projects (workstreams) page
agent: build
---

!`npm run page --silent -- projects`

If the operator asks about a specific project, run `npm run page --silent -- project/<id>` (once that page is supported by the renderer).
