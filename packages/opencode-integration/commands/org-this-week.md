---
description: Show what's happening this week — meetings, events, deadlines, hot tasks
agent: build
---

!`npm run page --silent -- this-week`

If the result is empty, briefly note that nothing's scheduled and offer to check `/org-projects` or `/dashboard` instead.
