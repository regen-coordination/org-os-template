# Dependencies
node_modules/
.pnp
.pnp.js

# Local linked repo clones
repos/*
!repos/README.md

# Build / output
dist
build
out
*.log

# Generated federation-map data (regenerated by `org-os-kms render map`)
data/kb/map.json

# Misc
.DS_Store
*.pem

# Local env files
.env
.env*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
__pycache__

# Worktrees (isolation for parallel development)
.worktrees/
.claude/worktrees/

# Scheduled-task lockfiles (per-machine state)
.claude/scheduled_tasks.lock

# Test gitignore fixtures
.gitignore.test

# Visual brainstorming sessions
.superpowers/

# Git worktrees (isolated workspaces)
.worktrees/

# Multica bridge local config (machine-specific path + credentials-adjacent ids)
packages/multica-bridge/config.yaml

# Symbient habitat (operator-private — see skills/symbient/SKILL.md)
# Anchored: habitats live only at the repo root. An unanchored "symbient/"
# would also match skills/symbient/ — the public framework skill directory.
/symbient/
